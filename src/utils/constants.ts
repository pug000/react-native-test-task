import type { UserData } from 'src/ts/interfaces';

const userData: UserData = {
  name: 'Dmitry Saitov',
  rating: 0,
  review: 0,
  status: 'Beginner',
  avatarUrl: null,
};

export default userData;
